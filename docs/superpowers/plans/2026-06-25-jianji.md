# 简记（jianji）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 构建一个微信小程序个人记账应用，支持快速记账、按日查看、月度统计、离线同步和 5 套可切换主题。

**Architecture:** Taro 4 + React + TypeScript 单仓小程序；微信云开发提供数据库与云函数；本地 wx.storage 离线优先，pendingQueue 驱动后台同步；CSS 变量 + ThemeContext 实现主题切换。

**Tech Stack:** Taro 4, React 18, TypeScript, 微信云开发, echarts-for-weixin, Jest（纯函数单测）

**Spec:** `docs/superpowers/specs/2026-06-25-jianji-design.md`

---

## 文件结构总览

```
jianji/
├── src/
│   ├── app.ts / app.config.ts / app.scss
│   ├── types/index.ts                    # Record, Category, User, ThemeId
│   ├── constants/
│   │   ├── storage-keys.ts
│   │   └── default-categories.ts
│   ├── themes/
│   │   ├── tokens.ts                     # 5 套主题 token 定义
│   │   └── apply-theme.ts                # 注入 CSS 变量
│   ├── utils/
│   │   ├── date.ts                       # 分组、格式化
│   │   ├── amount.ts                     # 金额格式化
│   │   ├── stats.ts                      # 月度聚合
│   │   └── id.ts                         # 本地 ID 生成
│   ├── services/
│   │   ├── storage.ts                    # wx.storage 封装
│   │   ├── sync.ts                       # 离线队列 + 云同步
│   │   ├── auth.ts                       # wx.login + 用户态
│   │   ├── records.ts                    # 记录 CRUD
│   │   └── categories.ts                 # 分类 CRUD
│   ├── contexts/
│   │   ├── ThemeContext.tsx
│   │   └── AuthContext.tsx
│   ├── components/
│   │   ├── TabBar/                       # 3 Tab + 居中 +
│   │   ├── ProfileEntry/                 # 首页顶栏「我的」
│   │   ├── SummaryCard/                  # 月度汇总（主题感知）
│   │   ├── RecordList/                   # 按日期分组列表
│   │   ├── RecordItem/
│   │   ├── NumPad/                       # 数字键盘
│   │   ├── CategoryGrid/
│   │   ├── DonutChart/                   # echarts 环形图
│   │   └── RankList/                     # 分类排行榜
│   └── pages/
│       ├── index/                        # 明细（首页）
│       ├── add/                          # 记账
│       ├── stats/                        # 统计
│       ├── profile/                      # 我的
│       ├── theme/                        # 外观主题
│       ├── categories/                   # 分类管理
│       └── login/                        # 登录授权
├── cloud/
│   └── functions/
│       ├── login/                        # openid 换 session
│       └── decryptPhone/                 # 手机号解密
├── __tests__/
│   ├── utils/date.test.ts
│   ├── utils/stats.test.ts
│   └── services/sync.test.ts
├── project.config.json                   # 微信开发者工具配置
└── package.json
```

---

### Task 1: 项目脚手架

**Files:**
- Create: `package.json`, `tsconfig.json`, `project.config.json`, `src/app.config.ts`
- Create: `src/app.ts`, `src/app.scss`

- [ ] **Step 1: 初始化 Taro 项目**

```bash
cd /Users/amy/Work/git/vibe-coding/jianji
npx @tarojs/cli@4 init . --template default --typescript --css sass --compiler vite --yes
```

若目录非空提示冲突，改用：

```bash
npx @tarojs/cli@4 init jianji-tmp --template default --typescript --css sass --compiler vite --yes
# 将生成文件移入根目录后删除 jianji-tmp
```

- [ ] **Step 2: 配置 app 路由**

`src/app.config.ts`:

```typescript
export default defineAppConfig({
  pages: [
    'pages/index/index',
    'pages/add/index',
    'pages/stats/index',
    'pages/profile/index',
    'pages/theme/index',
    'pages/categories/index',
    'pages/login/index',
  ],
  window: {
    backgroundTextStyle: 'light',
    navigationBarBackgroundColor: '#1c1917',
    navigationBarTitleText: '简记',
    navigationBarTextStyle: 'white',
    navigationStyle: 'custom',
  },
  cloud: true,
})
```

- [ ] **Step 3: 安装依赖**

```bash
npm install echarts-for-weixin
npm install -D jest @types/jest ts-jest
```

- [ ] **Step 4: 配置 Jest**

`jest.config.js`:

```javascript
module.exports = {
  preset: 'ts-jest',
  testEnvironment: 'node',
  roots: ['<rootDir>/__tests__'],
  moduleNameMapper: { '^@/(.*)$': '<rootDir>/src/$1' },
}
```

`package.json` scripts 添加 `"test": "jest"`。

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "chore: 初始化 Taro 4 微信小程序脚手架"
```

---

### Task 2: 类型与常量

**Files:**
- Create: `src/types/index.ts`
- Create: `src/constants/storage-keys.ts`
- Create: `src/constants/default-categories.ts`

- [ ] **Step 1: 定义核心类型**

`src/types/index.ts`:

```typescript
export type ThemeId = 'warm' | 'mint' | 'dark' | 'candy' | 'caramel'
export type RecordType = 'expense' | 'income'
export type SyncStatus = 'synced' | 'pending' | 'deleted'

export interface User {
  _id?: string
  openid: string
  phone?: string
  theme: ThemeId
  avatarUrl?: string
  nickName?: string
  createdAt?: string
}

export interface Category {
  _id: string
  userId: string
  name: string
  icon: string
  type: RecordType
  sort: number
  isDefault: boolean
}

export interface Record {
  _id: string
  userId: string
  type: RecordType
  amount: number
  categoryId: string
  note: string
  date: string
  createdAt: string
  updatedAt: string
  syncStatus: SyncStatus
}

export interface PendingOp {
  id: string
  collection: 'records' | 'categories'
  action: 'create' | 'update' | 'delete'
  payload: Record | Category
  createdAt: string
}

export interface ThemeTokens {
  pageBg: string
  surface: string
  surfaceBorder: string
  textPrimary: string
  textSecondary: string
  primary: string
  primaryGradient: string
  btnGradient: string
  expense: string
  income: string
  glow?: string
}
```

- [ ] **Step 2: 定义 storage keys 与预设分类**

`src/constants/storage-keys.ts`:

```typescript
export const KEYS = {
  RECORDS: 'records',
  CATEGORIES: 'categories',
  PENDING_QUEUE: 'pendingQueue',
  THEME: 'theme',
  USER: 'user',
  LAST_SYNC_AT: 'lastSyncAt',
} as const
```

`src/constants/default-categories.ts` — 按 spec §4.2 列出 7 支出 + 3 收入分类（餐饮🍜…其他）。

- [ ] **Step 3: Commit**

```bash
git add src/types src/constants
git commit -m "feat: 添加核心类型定义与预设分类常量"
```

---

### Task 3: 工具函数（TDD）

**Files:**
- Create: `src/utils/date.ts`, `src/utils/amount.ts`, `src/utils/stats.ts`, `src/utils/id.ts`
- Create: `__tests__/utils/date.test.ts`, `__tests__/utils/stats.test.ts`

- [ ] **Step 1: 写失败测试 — 日期分组**

`__tests__/utils/date.test.ts`:

```typescript
import { groupRecordsByDate, formatDateLabel } from '@/utils/date'
import type { Record } from '@/types'

const base = (date: string): Record => ({
  _id: '1', userId: 'u', type: 'expense', amount: 10,
  categoryId: 'c', note: '', date, createdAt: '', updatedAt: '', syncStatus: 'synced',
})

describe('groupRecordsByDate', () => {
  it('按日期倒序分组', () => {
    const groups = groupRecordsByDate([base('2026-06-24'), base('2026-06-25')])
    expect(groups[0].date).toBe('2026-06-25')
    expect(groups).toHaveLength(2)
  })
})

describe('formatDateLabel', () => {
  it('今天显示「今日」', () => {
    const today = new Date().toISOString().slice(0, 10)
    expect(formatDateLabel(today)).toBe('今日')
  })
})
```

- [ ] **Step 2: 运行确认失败**

```bash
npm test -- __tests__/utils/date.test.ts
```

Expected: FAIL

- [ ] **Step 3: 实现 date.ts**

```typescript
import type { Record } from '@/types'

export function groupRecordsByDate(records: Record[]) {
  const map = new Map<string, Record[]>()
  for (const r of records.filter(x => x.syncStatus !== 'deleted')) {
    if (!map.has(r.date)) map.set(r.date, [])
    map.get(r.date)!.push(r)
  }
  return [...map.entries()]
    .sort(([a], [b]) => b.localeCompare(a))
    .map(([date, items]) => ({ date, items }))
}

export function formatDateLabel(date: string): string {
  const today = new Date().toISOString().slice(0, 10)
  const yesterday = new Date(Date.now() - 86400000).toISOString().slice(0, 10)
  if (date === today) return '今日'
  if (date === yesterday) return '昨日'
  const [, m, d] = date.split('-')
  return `${Number(m)}月${Number(d)}日`
}

export function currentDateStr(): string {
  return new Date().toISOString().slice(0, 10)
}
```

- [ ] **Step 4: 写失败测试 — 月度统计**

`__tests__/utils/stats.test.ts`:

```typescript
import { calcMonthSummary, calcCategoryBreakdown } from '@/utils/stats'

describe('calcMonthSummary', () => {
  it('计算月收入支出结余', () => {
    const records = [
      { type: 'income', amount: 100, date: '2026-06-01', syncStatus: 'synced', categoryId: 'a' },
      { type: 'expense', amount: 30, date: '2026-06-15', syncStatus: 'synced', categoryId: 'b' },
    ] as any[]
    const r = calcMonthSummary(records, '2026-06')
    expect(r.income).toBe(100)
    expect(r.expense).toBe(30)
    expect(r.balance).toBe(70)
  })
})
```

- [ ] **Step 5: 实现 stats.ts 与 amount.ts、id.ts**

`src/utils/stats.ts` — `calcMonthSummary`, `calcCategoryBreakdown`（按 categoryId 聚合支出占比）。

`src/utils/amount.ts` — `formatAmount(n: number): string` 保留 2 位小数。

`src/utils/id.ts` — `genId(): string` 使用 `Date.now().toString(36) + Math.random().toString(36).slice(2)`.

- [ ] **Step 6: 运行全部测试**

```bash
npm test
```

Expected: PASS

- [ ] **Step 7: Commit**

```bash
git add src/utils __tests__
git commit -m "feat: 添加日期分组与统计工具函数"
```

---

### Task 4: 本地存储层

**Files:**
- Create: `src/services/storage.ts`

- [ ] **Step 1: 实现 storage 封装**

```typescript
import Taro from '@tarojs/taro'
import { KEYS } from '@/constants/storage-keys'
import type { Record, Category, PendingOp, ThemeId, User } from '@/types'

export const storage = {
  getRecords(): Record[] {
    return Taro.getStorageSync(KEYS.RECORDS) || []
  },
  setRecords(records: Record[]) {
    Taro.setStorageSync(KEYS.RECORDS, records)
  },
  getCategories(): Category[] {
    return Taro.getStorageSync(KEYS.CATEGORIES) || []
  },
  setCategories(categories: Category[]) {
    Taro.setStorageSync(KEYS.CATEGORIES, categories)
  },
  getPendingQueue(): PendingOp[] {
    return Taro.getStorageSync(KEYS.PENDING_QUEUE) || []
  },
  setPendingQueue(queue: PendingOp[]) {
    Taro.setStorageSync(KEYS.PENDING_QUEUE, queue)
  },
  getTheme(): ThemeId {
    return Taro.getStorageSync(KEYS.THEME) || 'warm'
  },
  setTheme(theme: ThemeId) {
    Taro.setStorageSync(KEYS.THEME, theme)
  },
  getUser(): User | null {
    return Taro.getStorageSync(KEYS.USER) || null
  },
  setUser(user: User | null) {
    if (user) Taro.setStorageSync(KEYS.USER, user)
    else Taro.removeStorageSync(KEYS.USER)
  },
}
```

- [ ] **Step 2: Commit**

```bash
git add src/services/storage.ts
git commit -m "feat: 添加 wx.storage 封装层"
```

---

### Task 5: 主题系统

**Files:**
- Create: `src/themes/tokens.ts`, `src/themes/apply-theme.ts`
- Create: `src/contexts/ThemeContext.tsx`

- [ ] **Step 1: 定义 5 套主题 token**

`src/themes/tokens.ts` — 按 spec §5.2–5.6 完整定义 `THEMES: Record<ThemeId, ThemeTokens>`。

暖阳（warm）示例：

```typescript
warm: {
  pageBg: '#1c1917',
  surface: '#292524',
  surfaceBorder: '#3d3835',
  textPrimary: '#fafaf9',
  textSecondary: '#a8a29e',
  primary: '#FFB800',
  primaryGradient: 'linear-gradient(135deg, #FFD100, #FFB800, #FF9500)',
  btnGradient: 'linear-gradient(135deg, #FFD100, #FF9500)',
  expense: '#fb923c',
  income: '#4ade80',
  glow: '0 0 24px rgba(255,184,0,0.2)',
}
```

- [ ] **Step 2: 实现 ThemeContext**

- 启动时从 `storage.getTheme()` 读取
- `setTheme(id)` → 更新 state + storage + 若已登录同步云端 `users.theme`
- 通过 inline style 或 CSS 变量注入到页面根节点

- [ ] **Step 3: 在 app.ts 包裹 ThemeProvider**

- [ ] **Step 4: Commit**

```bash
git add src/themes src/contexts/ThemeContext.tsx src/app.ts
git commit -m "feat: 实现 5 套主题切换系统"
```

---

### Task 6: 记录与分类服务

**Files:**
- Create: `src/services/records.ts`, `src/services/categories.ts`

- [ ] **Step 1: 实现 records.ts**

```typescript
export function addRecord(input: Omit<Record, '_id' | 'syncStatus' | 'createdAt' | 'updatedAt'>): Record {
  const now = new Date().toISOString()
  const record: Record = {
    ...input,
    _id: genId(),
    createdAt: now,
    updatedAt: now,
    syncStatus: 'pending',
  }
  const records = storage.getRecords()
  storage.setRecords([record, ...records])
  enqueueSync('records', 'create', record)
  triggerSync()
  return record
}

export function deleteRecord(id: string) { /* 标记 deleted + enqueue */ }
export function updateRecord(id: string, patch: Partial<Record>) { /* ... */ }
```

- [ ] **Step 2: 实现 categories.ts**

- `initDefaultCategories(userId)` — 首次注册创建预设分类
- `addCategory`, `updateCategory`, `deleteCategory` — 均走 offline-first

- [ ] **Step 3: Commit**

```bash
git add src/services/records.ts src/services/categories.ts
git commit -m "feat: 实现记录与分类离线 CRUD 服务"
```

---

### Task 7: 云开发与同步引擎

**Files:**
- Create: `cloud/functions/login/index.js`
- Create: `cloud/functions/decryptPhone/index.js`
- Create: `src/services/sync.ts`
- Create: `__tests__/services/sync.test.ts`

- [ ] **Step 1: 创建云函数 login**

```javascript
// cloud/functions/login/index.js
const cloud = require('wx-server-sdk')
cloud.init({ env: cloud.DYNAMIC_CURRENT_ENV })
exports.main = async () => {
  const { OPENID } = cloud.getWXContext()
  const db = cloud.database()
  const users = db.collection('users')
  const existing = await users.where({ openid: OPENID }).get()
  if (existing.data.length === 0) {
    await users.add({ data: { openid: OPENID, theme: 'warm', createdAt: db.serverDate() } })
  }
  return { openid: OPENID }
}
```

- [ ] **Step 2: 创建云函数 decryptPhone**（微信官方 getPhoneNumber 解密模板）

- [ ] **Step 3: 实现 sync.ts**

```typescript
export function enqueueSync(collection, action, payload) {
  const queue = storage.getPendingQueue()
  queue.push({ id: genId(), collection, action, payload, createdAt: new Date().toISOString() })
  storage.setPendingQueue(queue)
}

export async function triggerSync() {
  const network = await Taro.getNetworkType()
  if (network.networkType === 'none') return
  const user = storage.getUser()
  if (!user?.openid) return
  const queue = storage.getPendingQueue()
  const db = Taro.cloud.database()
  for (const op of queue) {
    // 根据 action 执行 add/update/remove
  }
  storage.setPendingQueue([])
  storage.setStorageSync(KEYS.LAST_SYNC_AT, Date.now())
}

export function setupNetworkListener() {
  Taro.onNetworkStatusChange(() => triggerSync())
}
```

- [ ] **Step 4: app.ts onLaunch 调用 setupNetworkListener + triggerSync**

- [ ] **Step 5: 写 sync 队列单测**（enqueue 逻辑）

- [ ] **Step 6: Commit**

```bash
git add cloud/ src/services/sync.ts __tests__/services
git commit -m "feat: 添加云函数与离线同步引擎"
```

---

### Task 8: 认证服务

**Files:**
- Create: `src/services/auth.ts`
- Create: `src/contexts/AuthContext.tsx`
- Create: `src/pages/login/index.tsx`

- [ ] **Step 1: 实现 auth.ts**

```typescript
export async function silentLogin(): Promise<User> {
  await Taro.cloud.init()
  const { result } = await Taro.cloud.callFunction({ name: 'login' })
  const { openid } = result as { openid: string }
  let user = storage.getUser()
  if (!user) {
    user = { openid, theme: storage.getTheme() }
    storage.setUser(user)
  }
  return user
}

export async function bindPhone(code: string): Promise<string> {
  const { result } = await Taro.cloud.callFunction({ name: 'decryptPhone', data: { code } })
  return (result as { phone: string }).phone
}
```

- [ ] **Step 2: AuthContext** — 提供 `user`, `login`, `bindPhone`, `isLoggedIn`

- [ ] **Step 3: login 页面** — 微信授权说明 + `getPhoneNumber` 按钮

- [ ] **Step 4: app.ts** — 启动时 `silentLogin()`，无手机号时引导 login 页

- [ ] **Step 5: Commit**

```bash
git add src/services/auth.ts src/contexts src/pages/login
git commit -m "feat: 实现微信授权登录与手机号绑定"
```

---

### Task 9: 公共组件

**Files:**
- Create: `src/components/TabBar/index.tsx`
- Create: `src/components/ProfileEntry/index.tsx`
- Create: `src/components/SummaryCard/index.tsx`
- Create: `src/components/RecordList/index.tsx`
- Create: `src/components/RecordItem/index.tsx`
- Create: `src/components/NumPad/index.tsx`
- Create: `src/components/CategoryGrid/index.tsx`

- [ ] **Step 1: TabBar** — 明细 | + | 统计；中间 + 使用 `btnGradient`；+ 点击 `Taro.navigateTo({ url: '/pages/add/index' })`

- [ ] **Step 2: ProfileEntry** — 右侧头像（`Image` 或默认图标）+「我的」文字；`onClick` → `/pages/profile/index`

- [ ] **Step 3: SummaryCard** — 暖阳主题：渐变光晕边框 + 渐变文字金额（spec §5.2 5 特色）；其他主题用各自 token

- [ ] **Step 4: RecordList** — 左侧暖黄时间轴（warm 主题）+ `groupRecordsByDate` 渲染

- [ ] **Step 5: RecordItem** — 图标 + 备注 + 分类 + 金额；swipe 删除用 `MovableView` 或长按菜单

- [ ] **Step 6: NumPad** — 3×4 数字键盘 + 小数点 + 删除

- [ ] **Step 7: CategoryGrid** — 4 列宫格，末位「+」跳转分类管理

- [ ] **Step 8: Commit**

```bash
git add src/components
git commit -m "feat: 添加 TabBar、ProfileEntry 等公共组件"
```

---

### Task 10: 明细页（首页）

**Files:**
- Create: `src/pages/index/index.tsx`, `index.scss`

- [ ] **Step 1: 布局**

```
[ProfileEntry 顶栏：简记 | 我的]
[SummaryCard 月度汇总]
[RecordList 按日分组]
[TabBar]
```

- [ ] **Step 2: 数据绑定** — 从 `storage.getRecords()` 读取，监听页面 `useDidShow` 刷新

- [ ] **Step 3: 左滑删除 / 点击编辑** — 编辑跳转 add 页并传入 record id

- [ ] **Step 4: 微信开发者工具预览验证**

- [ ] **Step 5: Commit**

```bash
git add src/pages/index
git commit -m "feat: 实现明细首页含我的入口"
```

---

### Task 11: 记账页

**Files:**
- Create: `src/pages/add/index.tsx`, `index.scss`

- [ ] **Step 1: 状态** — `type`, `amount`, `categoryId`, `note`, `date`（默认 `currentDateStr()`）

- [ ] **Step 2: UI** — 支出/收入切换 + 大金额 + CategoryGrid + NumPad + 日期选择器 + 完成按钮

- [ ] **Step 3: 完成逻辑** — 金额为空时按钮 disabled；调用 `addRecord()`；`Taro.navigateBack()`

- [ ] **Step 4: 编辑模式** — URL 参数 `?id=xxx` 时预填数据，完成时 `updateRecord`

- [ ] **Step 5: Commit**

```bash
git add src/pages/add
git commit -m "feat: 实现全屏记账页与数字键盘"
```

---

### Task 12: 统计页与图表

**Files:**
- Create: `src/pages/stats/index.tsx`
- Create: `src/components/DonutChart/index.tsx`
- Create: `src/components/RankList/index.tsx`

- [ ] **Step 1: 月份选择器** — 默认当月，picker 切换

- [ ] **Step 2: DonutChart** — echarts-for-weixin 环形图，中心显示总支出，色段按主题

- [ ] **Step 3: RankList** — `calcCategoryBreakdown` 渲染进度条 + 百分比

- [ ] **Step 4: Commit**

```bash
git add src/pages/stats src/components/DonutChart src/components/RankList
git commit -m "feat: 实现月度统计页与环形图"
```

---

### Task 13: 我的与主题页

**Files:**
- Create: `src/pages/profile/index.tsx`
- Create: `src/pages/theme/index.tsx`
- Create: `src/pages/categories/index.tsx`

- [ ] **Step 1: profile 页** — 菜单列表：外观主题、分类管理、同步状态（pendingQueue.length）、关于

- [ ] **Step 2: theme 页** — 5 套主题卡片（迷你预览 + 色板 + 选中 ✓）；点击调用 `setTheme`

- [ ] **Step 3: categories 页** — 列表展示分类；增删改表单；区分 expense/income tab

- [ ] **Step 4: Commit**

```bash
git add src/pages/profile src/pages/theme src/pages/categories
git commit -m "feat: 实现我的、主题切换与分类管理页"
```

---

### Task 14: 收尾与联调

**Files:**
- Modify: `src/app.ts`, `src/app.scss`, `project.config.json`

- [ ] **Step 1: 全局样式** — 使用主题 CSS 变量；页面 `background: var(--page-bg)`

- [ ] **Step 2: 离线提示** — 记账后若离线 `Taro.showToast({ title: '已保存，待同步', icon: 'none' })`

- [ ] **Step 3: 云数据库建表** — 微信开发者工具创建 `users`、`categories`、`records` 集合，设置权限为「仅创建者可读写」

- [ ] **Step 4: 联调清单（手动）**

  - [ ] 离线记账 → 开网后同步
  - [ ] 主题切换即时生效
  - [ ] 首页「我的」入口可跳转
  - [ ] 补记历史日期分组正确
  - [ ] 统计数据与明细一致

- [ ] **Step 5: 运行单测**

```bash
npm test
```

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "chore: 联调收尾与全局样式"
```

---

## Spec 覆盖自检

| Spec 章节 | 对应 Task |
|-----------|-----------|
| §2 技术架构 | Task 1, 7, 8 |
| §3.1 底栏 3 Tab | Task 9 TabBar |
| §3.2 首页 + 我的入口 | Task 9 ProfileEntry, Task 10 |
| §3.3 记账页 | Task 11 |
| §3.4 统计页 | Task 12 |
| §3.5 我的 | Task 13 |
| §4 数据模型 | Task 2, 4, 6, 7 |
| §5 主题系统 | Task 5, 13 |
| §6 错误处理 | Task 7, 11, 14 |
| §7 测试要点 | Task 3, 7, 14 |

无 TBD / 占位符。

---

## 执行方式

Plan complete and saved to `docs/superpowers/plans/2026-06-25-jianji.md`. Two execution options:

**1. Subagent-Driven（推荐）** — 每个 Task 派发独立 subagent，任务间 review，迭代快

**2. Inline Execution** — 当前会话按 Task 顺序执行，批次间设检查点

Which approach?
